## ⚙️ Установка и запуск

### 📋 Требования

* **Node.js** версии **16+**
* **npm**
* **Git**

Проверка:

```bash
node -v
npm -v
git --version
```

---

### 📥 Клонирование репозитория

```bash
git clone https://github.com/TeemUr-Check/CyberDoc.git
cd CyberDoc
```

---

### 📦 Установка зависимостей

Проект использует сторонние библиотеки, подключаемые напрямую в коде.
Перед запуском необходимо установить их вручную.

Инициализация проекта:

```bash
npm init -y
```

Установка зависимостей:

```bash
npm install express axios cheerio js-beautify
```

Используемые встроенные модули Node.js (устанавливать не нужно):

* `tls`
* `net`
* `path`

---

### ▶️ Запуск приложения

```bash
node server.js
```

После запуска сервер будет доступен локально `http://localhost:3000`.

---


